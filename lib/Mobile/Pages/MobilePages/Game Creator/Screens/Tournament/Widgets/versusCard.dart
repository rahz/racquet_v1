import 'package:flutter/material.dart';

class VersusCard extends StatefulWidget {
  const VersusCard({Key? key}) : super(key: key);

  @override
  VersusCardState createState() => VersusCardState();
}

class VersusCardState extends State<VersusCard> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
